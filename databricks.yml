# This is a Databricks asset bundle definition for data_cleaning.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: data_cleaning
  uuid: df85d07b-8c2f-4939-a1a0-911a1518789e

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-92680d89-60f8.cloud.databricks.com
    variables:
      warehouse_id: ac242810554f5c70
      catalog: datacleaning
      schema: bronze

  prod:
    mode: production
    workspace:
      host: https://dbc-92680d89-60f8.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/shungubepearlzinhle@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/shungubepearlzinhle@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: ac242810554f5c70
      catalog: datacleaning
      schema: bronze
    permissions:
      - user_name: shungubepearlzinhle@gmail.com
        level: CAN_MANAGE
